wing/cmd_pos:
    type: std_msgs/Float64
    wing:
        min: -pi/2
        max: pi/2
rudder/cmd_pos:
    type: std_msgs/Float64
    rudder:
        min: -pi/4
        max: pi/4
thrust:
    type: std_msgs/Float64
    thrust:
        min: -400
        max: 400
